# serverlist

## Setup

Before running `generate_serverlist.py`, add these three files to this project
folder (they are git-ignored - not committed, since they contain real server
inventory data):

- `rightSizing.xlsx`
- `tracker.xlsx`
- `subnet.xlsx`
